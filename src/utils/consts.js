const userNameKey = '--username';

const commands = {
  exit: '.exit',
  up: 'up',
  cd: 'cd',
  ls: 'ls',
  add: 'add',
  cat: 'cat',
  rn: 'rn',
  cp: 'cp',
  mv: 'mv',
  rm: 'rm',
  os: 'os',
};

export { userNameKey, commands };
